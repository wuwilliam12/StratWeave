# Optional: override database (default: SQLite in project root)
# DATABASE_URL=sqlite:///./stratweave.db
# DATABASE_URL=postgresql://user:password@localhost:5432/stratweave

# Optional: log SQL queries
# SQL_ECHO=true
